# Architecture-specific settings for arm64 (aarch64) servers.
# arm64 platforms boot via UEFI only, so no BIOS/GRUB packages are needed.

[Match]
Architecture=arm64

[Content]
Packages=
        linux-image-arm64
